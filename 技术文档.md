# 技术文档

## 1. 总体技术选型

| 层级 | 技术栈 |
|------|--------|
| 前端 | Vue 3.4 + TypeScript + Vite 5 + Pinia + Vue Router 4 + Axios |
| UI 组件库 | **Headless UI + Tailwind CSS**（C端）/ Element Plus（管理后台） |
| 代码编辑/渲染 | Monaco Editor (输入), markdown-it + highlight.js (渲染) |
| 构建部署 | Docker + Nginx (静态资源), 配合 CDN |
| 后端 | Spring Boot 2.6.13 + Spring Cloud Alibaba 2021.0.5.0 |
| JDK版本 | **JDK 17** |
| ORM框架 | **MyBatis-Plus 3.5.3.1** |
| 工具类库 | **Hutool 5.8.11** |
| 安全框架 | **Spring Security + JWT** |
| 接口文档 | **Knife4j 4.0.0 + Swagger** |
| 注册/配置中心 | Nacos 2.x |
| 服务网关 | Spring Cloud Gateway |
| 服务调用 | OpenFeign + LoadBalancer |
| 熔断限流 | Sentinel 1.8.6 |
| 分布式事务 | Seata 1.6.1 (AT模式，按需) |
| 消息队列 | RabbitMQ 3.10+ |
| 定时任务 | XXL-JOB 2.3.1 |
| 链路追踪 | SkyWalking 8.x |
| 监控 | Spring Boot Admin + Prometheus + Grafana |
| 存储 | MySQL 8.0 (主从), Redis 6.x (主从+哨兵), Elasticsearch 7.x (搜索) |
| 对象存储 | 阿里云 OSS / MinIO，CDN 加速 |
| 缓存框架 | Caffeine (本地) + Redis (分布式) |

## 2. 项目结构

### 2.1 后端项目结构

```
juejin-backend/
├── pom.xml                                    # 父POM，统一管理依赖版本
├── juejin-framework/                          # 框架层 - 基础设施和公共组件
│   ├── pom.xml
│   ├── juejin-common/                         # 公共核心模块
│   │   ├── 统一响应结果 (Result)
│   │   ├── 异常处理 (BusinessException, GlobalExceptionHandler)
│   │   ├── 基础实体 (BaseEntity)
│   │   ├── 分页封装 (PageParam, PageResult)
│   │   └── 通用常量、枚举
│   ├── juejin-starter-mybatis/                # MyBatis-Plus增强Starter
│   │   ├── 分页插件配置
│   │   ├── 自动填充处理器
│   │   ├── 多租户拦截器
│   │   └── 性能分析插件
│   ├── juejin-starter-redis/                  # Redis增强Starter
│   │   ├── RedisTemplate配置
│   │   ├── RedissonClient配置
│   │   ├── 缓存注解封装
│   │   └── Redis工具类封装
│   ├── juejin-starter-mq/                     # 消息队列Starter
│   │   ├── RabbitMQ配置
│   │   ├── 消息队列常量
│   │   ├── 生产者模板
│   │   └── 消费者基础类
│   ├── juejin-starter-security/               # 安全认证Starter
│   │   ├── Spring Security配置
│   │   ├── JWT Token生成与验证
│   │   ├── 认证过滤器
│   │   └── 权限注解支持
│   └── juejin-starter-swagger/                # 接口文档Starter
│       ├── Knife4j配置
│       ├── Swagger属性配置
│       └── API文档聚合
├── juejin-gateway/                            # API网关服务
│   ├── 路由配置
│   ├── JWT认证过滤器
│   ├── 限流配置
│   └── 跨域配置
├── juejin-user-service/                       # 用户服务
├── juejin-content-service/                    # 内容服务
├── juejin-social-service/                     # 社交服务
└── juejin-operation-service/                  # 运营服务
```

### 2.2 框架层设计说明

**juejin-framework** 是整个后端项目的基础设施层，包含：

1. **juejin-common**: 所有服务共享的公共代码
2. **juejin-starter-***: 可复用的Spring Boot Starter组件
   - 遵循Spring Boot自动配置规范
   - 提供spring.factories自动装配
   - 支持条件化配置

## 3. 微服务拆分与职责

**初期采用5个核心服务，降低复杂度：**

| 服务名 | 职责描述 | 主要存储依赖 |
|--------|----------|--------------|
| gateway-service | 统一入口，路由转发，鉴权校验，跨域处理，限流 | Nacos |
| user-service | 用户认证、用户信息、关注粉丝、成长体系 | MySQL, Redis |
| content-service | 文章管理、标签体系、专栏、搜索、审核流程 | MySQL, ES, Redis |
| social-service | 点赞、收藏、评论、沸点、Feed流、消息通知 | MySQL, Redis |
| operation-service | 签到任务、热榜计算、文件资源、定时任务调度 | MySQL, Redis, OSS |

**后期可拆分：**
- content-service -> content-service + search-service
- social-service -> social-service + message-service + feed-service
- operation-service -> operation-service + resource-service + job-service

## 4. 核心组件使用规范

### 4.1 Hutool工具类使用规范

**全面使用Hutool替换自定义工具类：**

| 功能 | Hutool工具类 | 说明 |
|------|-------------|------|
| Bean转换 | BeanUtil | 对象属性拷贝 |
| 字符串处理 | StrUtil | 字符串判空、截取、格式化 |
| 集合处理 | CollUtil | 集合判空、转换、过滤 |
| 日期处理 | DateUtil, LocalDateTimeUtil | 日期格式化、计算 |
| 加密解密 | SecureUtil, BCrypt | MD5、AES、BCrypt加密 |
| JSON处理 | JSONUtil | JSON序列化/反序列化 |
| HTTP请求 | HttpUtil | HTTP请求工具 |
| 文件处理 | FileUtil | 文件读写操作 |
| 验证码 | CaptchaUtil | 图形验证码生成 |
| 唯一ID | IdUtil | UUID、Snowflake |

**示例：**
```java
// Bean转换
UserVO vo = BeanUtil.copyProperties(user, UserVO.class);

// 字符串判空
if (StrUtil.isBlank(email)) { }

// BCrypt加密
String encryptPwd = BCrypt.hashpw(password);

// 日期格式化
String dateStr = DateUtil.format(new Date(), "yyyy-MM-dd HH:mm:ss");
```

### 4.2 Spring Security使用规范

**认证流程：**
1. 用户登录 -> 验证用户名密码 -> 生成JWT Token
2. 请求携带Token -> Gateway解析Token -> 转发到服务
3. 服务从Header获取userId -> 查询用户权限 -> 执行业务

**授权方式：**
- 方法级授权：使用@PreAuthorize注解
- URL级授权：在SecurityConfig中配置
- 数据级授权：在Service层进行权限校验

**示例：**
```java
@PreAuthorize("hasRole('ADMIN')")
@Operation(summary = "删除用户")
@DeleteMapping("/{id}")
public Result<Void> deleteUser(@PathVariable Long id) { }
```

### 4.3 Knife4j接口文档使用规范

**Controller注解：**
```java
@Tag(name = "用户服务", description = "用户相关接口")
@RestController
@RequestMapping("/api/v1/users")
public class UserController { }
```

**方法注解：**
```java
@Operation(summary = "用户登录", description = "用户登录接口")
@PostMapping("/login")
public Result<LoginVO> login(@RequestBody @Validated LoginDTO dto) { }
```

**DTO注解：**
```java
@Schema(description = "登录请求DTO")
public class LoginDTO {
    @Schema(description = "邮箱", required = true, example = "user@example.com")
    @NotBlank(message = "邮箱不能为空")
    private String email;
}
```

**访问地址：**
- 开发环境：http://localhost:8080/doc.html
- Gateway聚合：http://localhost:8080/v3/api-docs

## 5. 后端架构设计

### 5.1 技术版本锁定

```xml
<!-- pom.xml 核心依赖版本 -->
<properties>
    <java.version>17</java.version>
    <spring-boot.version>2.6.13</spring-boot.version>
    <spring-cloud-alibaba.version>2021.0.5.0</spring-cloud-alibaba.version>
    <mybatis-plus.version>3.5.3.1</mybatis-plus.version>
    <hutool.version>5.8.11</hutool.version>
    <knife4j.version>4.0.0</knife4j.version>
    <druid.version>1.2.16</druid.version>
    <redisson.version>3.19.0</redisson.version>
</properties>
```

### 5.2 MyBatis-Plus 配置

```yaml
# application.yml
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
    cache-enabled: true
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.juejin.*.entity
```

### 5.3 实体类示例

```java
// entity/User.java
@Data
@EqualsAndHashCode(callSuper = true)
@TableName("user")
@Schema(description = "用户实体")
public class User extends BaseEntity {
    
    @Schema(description = "邮箱")
    private String email;
    
    @Schema(description = "密码")
    private String password;
    
    @Schema(description = "昵称")
    private String nickname;
    
    @Schema(description = "状态")
    private Integer status;
    
}
```

### 5.4 Service层示例

```java
// service/impl/UserServiceImpl.java
@Service
@RequiredArgsConstructor
@Slf4j
public class UserServiceImpl extends ServiceImpl<UserMapper, User> implements UserService {
    
    private final UserMapper userMapper;
    private final RedisTemplate<String, Object> redisTemplate;
    
    @Override
    @Transactional(rollbackFor = Exception.class)
    public UserVO register(RegisterDTO dto) {
        // 检查邮箱是否已注册
        if (lambdaQuery().eq(User::getEmail, dto.getEmail()).exists()) {
            throw new BusinessException(ErrorCode.EMAIL_ALREADY_EXISTS);
        }
        
        // 创建用户 - 使用Hutool的BeanUtil
        User user = BeanUtil.copyProperties(dto, User.class);
        user.setPassword(BCrypt.hashpw(dto.getPassword()));
        user.setLevel(1);
        user.setPoints(0);
        
        save(user);
        log.info("User registered: {}", user.getId());
        
        return BeanUtil.copyProperties(user, UserVO.class);
    }
    
}
```

### 5.5 Controller层示例

```java
// controller/UserController.java
@Tag(name = "用户服务", description = "用户相关接口")
@RestController
@RequestMapping("/api/v1/users")
@RequiredArgsConstructor
public class UserController {
    
    private final UserService userService;
    
    @Operation(summary = "用户注册")
    @PostMapping("/register")
    public Result<UserVO> register(@RequestBody @Validated RegisterDTO dto) {
        UserVO userVO = userService.register(dto);
        return Result.success(userVO);
    }
    
    @Operation(summary = "用户登录")
    @PostMapping("/login")
    public Result<LoginVO> login(@RequestBody @Validated LoginDTO dto) {
        LoginVO loginVO = userService.login(dto);
        return Result.success(loginVO);
    }
    
    @Operation(summary = "获取当前用户信息")
    @GetMapping("/current")
    public Result<UserVO> getCurrentUser(@RequestHeader("X-User-Id") Long userId) {
        UserVO userVO = userService.getUserById(userId);
        return Result.success(userVO);
    }
    
}
```

## 6. 接口设计规范

### 6.1 通用约定

- **基础路径**：`/api/v1`
- **鉴权方式**：请求头 `Authorization: Bearer <access_token>`
- **用户ID传递**：请求头 `X-User-Id: <user_id>`
- **统一响应格式**：

```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "timestamp": 1704067200000
}
```

### 6.2 错误码定义

| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权（Token无效或过期） |
| 403 | 禁止访问（无权限） |
| 404 | 资源不存在 |
| 429 | 请求过于频繁 |
| 500 | 服务器内部错误 |
| 1001 | 邮箱已注册 |
| 1002 | 手机号已注册 |
| 1003 | 用户名或密码错误 |
| 1004 | 账号已被禁用 |

### 6.3 分页参数规范

```java
@Data
@Schema(description = "分页参数")
public class PageParam {
    
    @Schema(description = "页码", defaultValue = "1")
    @Min(value = 1)
    private Integer page = 1;
    
    @Schema(description = "每页大小", defaultValue = "20")
    @Min(value = 1)
    @Max(value = 100)
    private Integer size = 20;
    
    @Schema(description = "排序字段")
    private String sort;
    
    @Schema(description = "游标（用于Feed流）")
    private Long cursor;
    
}
```

## 7. 前端架构设计

### 7.1 C端UI方案选择

**推荐方案：Headless UI + Tailwind CSS**

| 对比项 | Element Plus | Headless UI + Tailwind |
|--------|--------------|------------------------|
| 样式定制 | 需覆盖CSS变量，难度大 | 完全自定义，灵活度高 |
| 包体积 | 较大（全量引入） | 按需引入，体积小 |
| 设计一致性 | 组件风格固定 | 完全可控，品牌感强 |
| 开发效率 | 高（组件丰富） | 中（需自行封装） |
| 适用场景 | 管理后台 | C端用户界面 |

**结论：** C端页面追求独特设计和用户体验，建议使用 **Headless UI + Tailwind CSS**，管理后台使用 Element Plus。

### 7.2 前端技术栈

```
C端 (用户端):
├── 组件库: Headless UI (无样式组件)
├── 样式: Tailwind CSS 3.x + PostCSS
├── 图标: Heroicons / Phosphor Icons
├── 动画: Framer Motion / Vue Motion
└── 工具: @tailwindcss/typography (文章排版)

管理后台:
├── 组件库: Element Plus 2.x
├── 样式: 覆盖Element主题变量
└── 图表: ECharts 5.x
```

### 7.3 目录结构

```
src/
├── api/                    # 接口请求封装
│   ├── request.ts          # Axios 实例、拦截器
│   ├── userApi.ts          # 用户相关 API
│   ├── contentApi.ts       # 内容相关 API
│   ├── socialApi.ts        # 社交相关 API
│   ├── feedApi.ts          # Feed 流 API
│   ├── searchApi.ts        # 搜索 API
│   └── messageApi.ts       # 消息 API
├── assets/                 # 静态资源
│   ├── styles/             # 全局样式
│   │   ├── tailwind.css    # Tailwind基础导入
│   │   └── variables.scss  # SCSS变量
│   └── icons/              # 图标资源
├── components/             # 可复用组件
│   ├── common/             # 全局通用组件
│   │   ├── AppButton.vue   # 自定义按钮
│   │   ├── AppInput.vue    # 自定义输入框
│   │   ├── AppModal.vue    # 弹窗组件
│   │   ├── AppDropdown.vue # 下拉菜单
│   │   ├── Navbar.vue      # 导航栏
│   │   ├── Sidebar.vue     # 侧边栏
│   │   ├── Avatar.vue      # 头像组件
│   │   └── Skeleton.vue    # 骨架屏
│   ├── article/            # 文章相关组件
│   │   ├── ArticleCard.vue       # 文章卡片
│   │   ├── ArticleList.vue       # 文章列表
│   │   ├── MarkdownRenderer.vue  # Markdown渲染
│   │   ├── CodeBlock.vue         # 代码块
│   │   ├── ArticleToc.vue        # 文章目录
│   │   └── ArticleMeta.vue       # 文章元信息
│   ├── social/             # 社交组件
│   │   ├── LikeButton.vue        # 点赞按钮
│   │   ├── FavoriteButton.vue    # 收藏按钮
│   │   ├── CommentSection.vue    # 评论区
│   │   ├── CommentItem.vue       # 评论项
│   │   └── PinCard.vue           # 沸点卡片
│   └── editor/             # 编辑器组件
│       ├── MarkdownEditor.vue    # Markdown编辑器
│       └── EditorToolbar.vue     # 编辑器工具栏
├── composables/            # 组合式函数
│   ├── useAuth.ts          # 认证逻辑
│   ├── useUser.ts          # 用户信息
│   ├── useWebSocket.ts     # WebSocket连接
│   ├── useInfiniteScroll.ts # 无限滚动
│   ├── useLike.ts          # 点赞逻辑
│   ├── useFavorite.ts      # 收藏逻辑
│   └── useSearch.ts        # 搜索逻辑
├── layouts/                # 布局组件
│   ├── DefaultLayout.vue   # 默认布局（导航+内容）
│   ├── ArticleLayout.vue   # 文章阅读布局
│   └── AdminLayout.vue     # 后台布局
├── router/                 # 路由配置
│   ├── index.ts            # 路由入口
│   ├── routes.ts           # 路由定义
│   └── guards.ts           # 路由守卫
├── stores/                 # Pinia 状态管理
│   ├── index.ts            # Store入口
│   ├── user.ts             # 用户状态
│   ├── feed.ts             # Feed流状态
│   ├── article.ts          # 文章状态
│   ├── notification.ts     # 通知状态
│   └── settings.ts         # 设置状态
├── views/                  # 页面视图
│   ├── home/               # 首页
│   │   ├── HomePage.vue
│   │   ├── RecommendedTab.vue
│   │   └── FollowingTab.vue
│   ├── article/            # 文章
│   │   ├── ArticleDetail.vue
│   │   └── ArticleEditor.vue
│   ├── user/               # 用户
│   │   ├── UserProfile.vue
│   │   ├── UserArticles.vue
│   │   ├── UserFollowers.vue
│   │   └── UserFollowing.vue
│   ├── search/             # 搜索
│   │   └── SearchResult.vue
│   ├── social/             # 社交
│   │   ├── PinsPage.vue
│   │   ├── Notifications.vue
│   │   └── Messages.vue
│   └── dashboard/          # 个人中心
│       └── Dashboard.vue
├── utils/                  # 工具函数
│   ├── http.ts             # HTTP工具
│   ├── storage.ts          # 存储工具
│   ├── markdown.ts         # Markdown处理
│   ├── format.ts           # 格式化工具
│   └── validate.ts         # 验证工具
├── types/                  # TypeScript类型
│   ├── user.ts
│   ├── article.ts
│   ├── social.ts
│   └── api.ts
├── App.vue
└── main.ts
```

## 8. 部署与运维

### 8.1 环境规划

| 环境 | 用途 | 配置 |
|------|------|------|
| dev | 开发环境 | 单机部署，H2数据库 |
| test | 测试环境 | Docker Compose，MySQL+Redis |
| staging | 预发布 | K8s集群，生产数据镜像 |
| prod | 生产环境 | K8s集群，多可用区部署 |

### 8.2 Docker Compose（开发环境）

```yaml
version: '3.8'
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: juejin
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      
  redis:
    image: redis:6.2-alpine
    ports:
      - "6379:6379"
      
  rabbitmq:
    image: rabbitmq:3.10-management
    ports:
      - "5672:5672"
      - "15672:15672"
      
  elasticsearch:
    image: elasticsearch:7.17.0
    environment:
      - discovery.type=single-node
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    ports:
      - "9200:9200"
      
  nacos:
    image: nacos/nacos-server:v2.2.0
    environment:
      MODE: standalone
    ports:
      - "8848:8848"

volumes:
  mysql_data:
```

## 9. 安全策略

### 9.1 认证安全

- **密码加密**：BCrypt，强度10
- **Token策略**：
  - Access Token：有效期30分钟
  - Refresh Token：有效期7天，只能使用一次
- **登录安全**：
  - 连续5次失败锁定15分钟
  - 异地登录提醒
  - 单设备登录（可选）

### 9.2 接口安全

- **防重放攻击**：请求头携带时间戳，有效期5分钟
- **限流策略**：
  - 登录接口：5次/分钟
  - 普通接口：100次/分钟
  - 搜索接口：30次/分钟
- **敏感操作二次验证**：修改密码、绑定手机需要验证码

### 9.3 数据安全

- **传输加密**：全站HTTPS，TLS 1.3
- **敏感数据脱敏**：
  - 手机号：138****8888
  - 邮箱：a***@example.com
- **SQL注入防护**：预编译语句，参数化查询
- **XSS防护**：输入过滤，输出转义

## 10. 性能优化

### 10.1 缓存策略

| 数据 | 缓存位置 | 过期时间 | 更新策略 |
|------|----------|----------|----------|
| 用户信息 | Redis | 30分钟 | Cache-Aside |
| 文章详情 | Redis + Caffeine | 1小时 | Cache-Aside |
| 点赞数 | Redis | 永久 | 定时同步 |
| 热榜 | Redis | 1小时 | 定时计算 |
| Feed流 | Redis | 无 | 实时更新 |

### 10.2 数据库优化

- **索引优化**：
  - 用户表：email(唯一), phone(唯一)
  - 文章表：author_id, status, create_time
  - 关注表：user_id + following_id(联合唯一)
  
- **分表策略**：
  - 文章表：按时间分表（每月一张）
  - 评论表：按文章ID哈希分表

### 10.3 前端优化

- **代码分割**：路由懒加载
- **资源压缩**：Gzip/Brotli
- **图片优化**：WebP格式，懒加载
- **CDN加速**：静态资源CDN分发
- **缓存策略**：Service Worker离线缓存
